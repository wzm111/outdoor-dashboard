# 户外助手看板（Web / PWA）

> 当前版本：**v1.5.0-2026-07-14**。前端已拆分为模块化 JS，活动视图按运动类型（跑步/徒步/攀岩/骑行）展示专业字段；徒步表格新增「天数」列，支持结束日期与负重（重装/轻装/超轻）展示；攀岩/抱石录入不再强制填写距离；总览保留体能档案基准指标，身体年龄参考指标已移至报告中心；新增 Service Worker 自动检测新版本并提示刷新；视图切换保留 URL hash，刷新后停留在当前标签；移动端小屏下启用底部固定导航栏；**新增增量数据同步：首次全量拉取后，后续刷新只拉取自上次同步以来的变更与删除；离线队列增强：乐观锁冲突检测、自动压缩重复变更、指数退避重试；骨架屏 / Shimmer Loading：数据拉取阶段显示与最终布局近似的占位结构，感知加载更快；v1.0.1 热修：修复 render 时误删骨架屏容器导致刷新无加载态的问题；v1.0.2 热修：修复空缓存时仍走增量同步导致数据被覆盖为空的问题，并在页脚新增「清除缓存」兜底入口；v1.0.3 热修：增量同步后若 gear/routes/segments 仍为空，自动回退到全量同步补全，避免历史损坏缓存导致持续空白；v1.1.0 报告中心增强：新增装备重量分类（Base/Worn/Consumable）环形图、体能等级评估/山力评分、周报/月报周期摘要卡片，并改进 Segment PB 匹配逻辑；v1.2.0 路线详情增强：点击路线「详情」可查看完整统计、分段行程、下撤点/住宿、历史活动记录，若路线配置了可访问的 GPX URL，还会自动解析并绘制 Canvas 海拔剖面图；v1.3.0 报告中心增强：训练指标新增状态评估/CTL 趋势/周 TSS 汇总/活动 TSS 明细，新增训练负荷卡片（ACWR、疲劳评分、8 周距离历史柱状图），Segment PB 增加进步趋势与近期记录，Gear Health 增加磨损比例与维护记录；v1.3.1 交互优化：将身体年龄参考指标从总览统计卡片/体能档案网格移至报告中心，使总览只保留客观基准数据；v1.3.2 体验增强：活动表格点击行改为打开运动类型专属详情弹窗（统计、天气、不适、备注、装备），报告中心身体年龄新增历史趋势折线图；v1.3.3 移动端优化：640px 以下表格自动转为堆叠卡片，所有弹窗全屏化并适配安全区与软键盘，输入框字号提升至 16px 防止 iOS 自动缩放，新增弹窗 Escape 关闭与底层滚动锁定，PWA 支持横屏；v1.3.4 活动详情增强：活动详情弹窗新增 GPX 海拔剖面图，优先使用活动自身 GPX，其次自动匹配同名路线的 GPX，无需离开活动弹窗即可查看轨迹海拔剖面；**v1.3.5 双向跳转 + 身体不适标签中文化**：活动详情弹窗新增「查看路线详情」入口，路线详情弹窗的历史活动列表支持点击打开对应活动详情，形成活动 ↔ 路线的快速导航闭环；活动详情与体能档案中的身体不适标签（如 `it_band_left`、`blisters_right_foot`）统一翻译为中文展示，原始数据不变。**v1.3.6 海拔剖面交互增强**：活动与路线详情中的海拔剖面图支持鼠标/触摸滑动交互，实时显示当前位置的距离、海拔与累计爬升，并高亮当前点与垂直参考线。** **v1.3.7 装备 ↔ 活动双向跳转**：活动详情弹窗的装备列表与装备详情弹窗的「用过的活动」列表均支持点击整行打开对方详情，形成装备 ↔ 活动的快速导航闭环。** **v1.3.8 离线队列可视化**：顶部新增「同步状态」入口，可查看网络/快照/待同步条数，支持立即同步、压缩队列，失败/冲突项可单独重试或丢弃。** **v1.3.9 活动 GPX 上传**：活动编辑/新增表单支持上传本地 GPX 文件，以内嵌 base64 形式保存到 `data.gpx_file`，活动详情优先使用活动自身 GPX 渲染海拔剖面，1MB 大小限制，不覆盖距离/爬升/下降。**v1.4.0 训练负荷独立视图**：新增「训练负荷」独立标签页，集中展示 ACWR、疲劳评分、本周/本月距离、近 8 周距离/爬升柱状图、疲劳与酸痛 30 天趋势、周负荷汇总表。**v1.5.0 恢复独立视图**：新增「恢复」独立标签页，基于最近一次高强度活动自动生成恢复阶段日历、剩余恢复天数、今日活动/拉伸/注意事项建议。**

一个**纯静态、零构建步骤**的单页看板，直接读取你的 Supabase 数据，展示活动、装备、路线、身体趋势和计划。可作为 PWA 安装到手机/桌面，支持离线查看上次同步的快照。

## 它是什么

- 纯 vanilla JS + 一个 `<canvas>` 画折线，**无第三方库、无打包工具**，呼应本项目脚本侧 stdlib-only 的风格。
- 通过现有 Edge Function API 工作：
  1. 用 `api_secret` 调 `POST /api/auth/token` 换取 JWT；
  2. 用 JWT 调 `POST /api/sync {action:"export"}` 一次性拉全量数据；
  3. 客户端解包（对齐脚本侧 `_unwrap`）并渲染。

## 🔒 安全说明（重要）

- **密钥绝不写入代码仓库。** 仓库里的 `app.js` 只有一个默认 *API 地址*（非敏感），没有任何密钥。
- 访问密钥（`api_secret`）由你在浏览器界面输入，勾选「记住」后**仅存本浏览器的 `localStorage`**，不上传、不进 git。
- 点右上角 ⏏（断开）或清除浏览器站点数据即可移除密钥与本地快照。
- Service Worker **不缓存任何 API 响应**（含 token 和数据），只缓存静态外壳；离线数据回退用的是 `localStorage` 快照，由页面逻辑控制。
- 如果你把看板部署到**公开** URL：任何拿到该 URL 的人看到的只是登录框，没有密钥进不去；但请勿在公共电脑上勾选「记住密钥」。

## 本地运行

因为用了 Service Worker 和 `fetch`，建议用本地 HTTP 服务（直接 `file://` 打开时 SW 不可用，但核心功能仍可用）：

```bash
cd web
python3 -m http.server 8080
# 浏览器打开 http://localhost:8080
```

首屏填入：

- **API 地址**：`https://<你的项目>.supabase.co/functions/v1`（已预填默认值）
- **访问密钥**：你的 `api_secret`（与 `~/.outdoor-assistant/config.json` 里的一致）

点「连接」即可。

## 部署到 GitHub Pages（已上线）

本目录的前端**已部署**到独立公开仓库，线上地址：

**<https://wzm111.github.io/outdoor-dashboard/>**

部署架构（因主仓库 `outdoor-assistant` 为私有、免费账号私有仓库无法开 Pages）：

- 源码维护在**本私有主仓库**的 `web/` 目录（唯一正确源）。
- 一个独立**公开仓库** `wzm111/outdoor-dashboard` 仅托管前端 6 文件（无任何密钥），开 GitHub Pages（main 分支 / 根目录）。
- 改完本目录代码后，运行同步脚本把改动推到公开仓库、触发 Pages 重建：

```bash
bash web/sync-to-pages.sh            # 同步并推送
bash web/sync-to-pages.sh --dry-run  # 只看会同步什么，不推送
```

脚本会自动 clone 公开仓库、覆盖前端文件、**推送前扫描密钥**、仅在有改动时提交推送（带网络重试），跑完清理临时目录。前置：`gh` CLI 已登录（走 HTTPS token，无需 SSH）。

> Edge Function 已设 `Access-Control-Allow-Origin: *`，跨域无需额外配置。

## 应用图标

`manifest.json` 引用 `icon-192.png` / `icon-512.png` 作为 PWA 安装图标，仓库已附带（深墨绿圆角底 + 翠绿雪山峰，与页面主色一致）。它们由 `web/` 下用 Python 标准库（`zlib`+`struct` 手写最小 PNG 编码器）生成，无需 ImageMagick/PIL。

要换图标，放两张对应尺寸的方形 PNG 覆盖即可（有 ImageMagick 时 `magick source.png -resize 192x192 web/icon-192.png`），然后跑同步脚本。

> ⚠️ **改任何前端文件后，记得 bump `service-worker.js` 顶部的 `CACHE` 版本号**（如 `outdoor-dashboard-v2` → `v3`）。否则旧版外壳会一直从 Service Worker 缓存返回，用户看到的还是旧页面（缓存清理逻辑只删“非当前版本”的缓存，不会清掉同名缓存自身）。

## 视图说明

| 标签 | 内容 |
|------|------|
| 总览 | 关键统计卡片（总活动/里程/爬升/时长/近 30 天里程/装备数）+ **体能档案卡片**（等级徽章、指标网格、需关注提示、当前目标）+ 最近 5 次活动 |
| 活动 | 全部活动表格（640px 以下自动转为堆叠卡片），支持手动新增/编辑/删除 + AI 一句话录入；点击行打开运动类型专属详情弹窗（统计、天气、不适、备注、装备）；**支持同一天同路线保存多条记录，自动按 sequence 编号（#2/#3...）** |
| 身体趋势 | 体重 / 疲劳度 / 睡眠 / 肌肉酸痛 的折线图；支持手动新增/编辑/删除 + AI 一句话录入；身体日志表格小屏下自动转为卡片 |
| 装备 | 装备按类别分组卡片，支持展开详情、新增/编辑/删除、从网页抓取/粘贴规格/AI 识别参数更新装备；**新增拍照/扫码 AI 识别录入** |
| 路线 | 路线库表格（640px 以下自动转为堆叠卡片），支持查看详情、新增/编辑/删除 + AI 一句话录入 |
| 计划 | 行程计划 + 恢复计划卡片，支持手动新增/编辑/删除、由路线一键生成装备推荐；**支持按天数自动调整装备与消耗品，自动天气失败可手动兜底；新增 AI 价格追踪、Gear Health 生命周期洞察、历史感知推荐 reasoning** |
| 报告 | 训练指标（CTL/ATL/TSB）+ 状态评估/趋势/活动 TSS 明细；训练负荷（ACWR/疲劳评分/周距离历史）；身体年龄参考与历史趋势；装备重量分类/Gear Health/体能等级/Segment PB/本周最常用装备 |
| 训练负荷 | ACWR、疲劳评分、本周/本月距离、近 8 周距离/爬升柱状图、疲劳与酸痛 30 天趋势、周负荷汇总表 |
| 恢复 | 基于最近一次高强度活动生成恢复阶段日历、剩余恢复天数、今日活动/拉伸/注意事项建议 |

## 离线体验

- 联网时自动同步，离线时自动显示缓存状态与待同步提示。
- 离线期间的新增/编辑/删除会排队，恢复网络后自动后台同步。
- 页脚提供「导出/导入 JSON 备份」作为应急兜底。

## 装备更新

装备卡片上两个按钮：

- **详情**：展开装备全部字段（重量、材质、防水、价格、尺码、季节、地形等）。
- **更新**：通过三种方式补全/更新装备信息：
  1. **网页抓取**：填写 REI / 品牌官网商品 URL，由后端 Edge Function 代理抓取重量、材质、防水、价格、颜色尺码等字段。
  2. **AI 识别**：用自然语言描述装备（或粘贴规格文本），后端同时调用 Kimi（Moonshot）和 DeepSeek 自动提取结构化字段，取结果更完整的一方。
  3. **粘贴规格文本**：从京东/天猫商品详情页复制规格参数文本，前端自动解析重量、材质、防水、价格、颜色尺码、季节、地形等字段。

网页抓取和 AI 识别需要先在 Supabase 部署 `/api/scrape/gear` 与 `/api/ai/gear` 端点，参考 `web/scrape-gear-edge-function.ts` 与 `supabase/functions/api/index.ts`。部署前需在 Supabase 环境变量中配置 `MOONSHOT_API_KEY` 和 `DEEPSEEK_API_KEY`。

京东/天猫反爬较强，若抓取失败请优先使用 AI 识别或粘贴规格文本方式。

## 数据契约

看板消费 `POST /api/sync {action:"export"}` 的返回：

```json
{
  "profile": { "data": {...}, "raw_markdown": "..." },
  "gear":     [{ "slug": "...", "data": {...}, "raw_markdown": "..." }],
  "routes":   [{ "slug": "...", "name": "...", "data": {...} }],
  "activities":[{ "date": "...", "route": "...", "sequence": 0, "data": {...} }],
  "body_logs":[{ "date": "...", "data": {...} }],
  "plans":    [{ "plan_type": "...", "date": "...", "route": "...", "data": {...} }],
  "segments": [{ "slug": "...", "data": {...} }]
}
```

注意：export 返回的是**未解包**的 DB 行（`{slug/date, data, ...}`），看板用 `unwrap()` 在客户端展开成扁平字段，逻辑与脚本侧 `APIBackend._unwrap` 一致。
